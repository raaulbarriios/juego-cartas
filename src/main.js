import { GameController } from './controllers/GameController.js';

window.addEventListener('DOMContentLoaded', () => {
    const app = new GameController();
    console.log('Card Clicker Game Initialized');
});
